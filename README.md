This project contains an end-to-end automation framework for a demo test.  
It is built with playwright and typeScript and a simple Page Object Model, and it automates the full flow of creating, adjusting, and submitting a Buildings & Contents insurance application.

Installation for playwright test

Make sure Node.js is installed run below in terminal to check node version
node -v
If you don’t have Node, download it from node website.

Starting a Playwright project
Run this Playwright init in your terminal

npm init playwright@latest

Choose TypeScript

Tests folder name

Add GitHub Actions workflow

Install Playwright browsers always choose yes
